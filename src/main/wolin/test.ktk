package pl.qus.wolin

var screen: ubyte[]^1024

fun print(what: string) {
    val i = 0
    val znak = what[i]
    while (znak != 0) {
        screen[i] = what[i]
        i++
    }
}

fun main() {
    print("dupa")
}
