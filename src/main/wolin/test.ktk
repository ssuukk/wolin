package pl.qus.wolin

var screen: ubyte[]^1024

var i: uword = 0

var chr: ubyte = 0

fun test(wart: ubyte): bool {
    if(wart > 10)
        return true
    else if(wart > 5)
        return false

    i++
    return true
}

fun main() {
    test(5)
    test(10)
}
